id: contact-card
purpose: "Present real contact routes (phone, email, address, booking)."
required_props:
  heading: string
  phone: string | null
  email: string | null
  address: string | null
  booking_url: string | null
accessibility:
  heading_level: h2
  image_alt_required: false
seo_notes:
  - "Use tel: and mailto: links; mark up address for LocalBusiness schema."
agent_rules:
  - "Render only contact routes present in the brief. Omit, never fabricate."
  - "If no route exists, leave a TODO and record it in missing-inputs.md."
